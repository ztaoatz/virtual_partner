# 邮件验证功能配置说明

## 📧 邮件服务配置

### 1. 更新Django设置文件

在 `settings.py` 中配置邮件服务（已完成）：

```python
# 邮件配置
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.qq.com'  # 根据实际邮箱服务商修改
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = 'your_email@qq.com'  # 替换为实际邮箱
EMAIL_HOST_PASSWORD = 'your_smtp_password'  # 替换为邮箱授权码
DEFAULT_FROM_EMAIL = 'Virtual Partner <your_email@qq.com>'
```

### 2. 支持的邮箱服务商配置

#### QQ邮箱
```python
EMAIL_HOST = 'smtp.qq.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
```

#### 163邮箱
```python
EMAIL_HOST = 'smtp.163.com'
EMAIL_PORT = 465
EMAIL_USE_SSL = True
```

#### Gmail
```python
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
```

#### 企业邮箱
```python
EMAIL_HOST = 'smtp.exmail.qq.com'  # 腾讯企业邮箱
EMAIL_PORT = 465
EMAIL_USE_SSL = True
```

### 3. 获取邮箱授权码

#### QQ邮箱
1. 登录QQ邮箱
2. 点击"设置" -> "账户"
3. 找到"POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV服务"
4. 开启"POP3/SMTP服务"
5. 生成授权码，用于EMAIL_HOST_PASSWORD

#### 163邮箱
1. 登录163邮箱
2. 点击"设置" -> "POP3/SMTP/IMAP"
3. 开启"POP3/SMTP服务"
4. 设置授权密码

### 4. 安全建议

1. **使用环境变量**：
   ```python
   import os
   EMAIL_HOST_USER = os.environ.get('EMAIL_HOST_USER')
   EMAIL_HOST_PASSWORD = os.environ.get('EMAIL_HOST_PASSWORD')
   ```

2. **创建.env文件**：
   ```
   EMAIL_HOST_USER=your_email@qq.com
   EMAIL_HOST_PASSWORD=your_smtp_password
   ```

3. **添加.env到.gitignore**：
   ```
   .env
   *.env
   ```

## 🔧 功能特性

### 1. 注册方式
- **用户名注册**：传统的用户名+密码注册
- **邮箱注册**：邮箱+用户名+密码+验证码注册（推荐）

### 2. 验证码功能
- **发送验证码**：6位数字验证码
- **验证码有效期**：5分钟
- **发送频率限制**：60秒倒计时
- **一次性使用**：验证码使用后自动失效

### 3. 密码重置
- **邮箱验证**：通过注册邮箱接收验证码
- **安全重置**：验证码确认后设置新密码

### 4. 邮件模板
- **美观设计**：响应式HTML邮件模板
- **品牌一致性**：与应用界面风格一致
- **多种类型**：注册验证、密码重置等

## 🚀 API接口

### 1. 发送验证码
```
POST /appp/send-verification-code/
Content-Type: application/json

{
  "email": "user@example.com",
  "type": "register" // register, password_reset, email_change
}
```

### 2. 验证邮箱验证码
```
POST /appp/verify-email-code/
Content-Type: application/json

{
  "email": "user@example.com",
  "code": "123456",
  "type": "register"
}
```

### 3. 注册（邮箱版）
```
POST /appp/register/
Content-Type: application/json

{
  "username": "testuser",
  "email": "user@example.com",
  "password": "password123",
  "verification_code": "123456",
  "nickname": "测试用户"
}
```

### 4. 重置密码
```
POST /appp/reset-password/
Content-Type: application/json

{
  "email": "user@example.com",
  "verification_code": "123456",
  "new_password": "newpassword123"
}
```

## 📱 前端页面

### 1. 注册页面 (/register)
- 模式切换：用户名注册 / 邮箱注册
- 验证码输入：倒计时按钮
- 实时验证：邮箱格式、密码确认
- 响应式设计：支持移动端

### 2. 密码重置页面 (/reset-password)
- 邮箱输入：格式验证
- 验证码发送：倒计时限制
- 密码设置：强度检查
- 用户友好：清晰的操作指引

### 3. 登录页面 (/login)
- 忘记密码链接：跳转到重置页面
- 兼容性：支持原有用户名登录
- 状态显示：邮箱验证状态

## 🛠 部署指南

### 1. 数据库迁移
```bash
cd backend/Lingxi\ -\ fuben
python manage.py makemigrations users
python manage.py migrate
```

### 2. 邮件服务配置
1. 更新 `settings.py` 中的邮件配置
2. 设置环境变量或直接配置
3. 测试邮件发送功能

### 3. 前端部署
```bash
cd frontend/vue301
npm install
npm run dev  # 开发环境
npm run build  # 生产环境
```

## 🧪 测试

### 1. 后端测试
```python
# 测试发送验证码
python manage.py shell
from users.email_service import EmailService
result = EmailService.send_verification_code('test@example.com', 'register')
print(result)
```

### 2. 功能测试
1. 注册新用户（邮箱模式）
2. 接收并使用验证码
3. 测试密码重置功能
4. 验证登录状态

## 🔍 故障排除

### 1. 邮件发送失败
- 检查邮箱配置是否正确
- 确认授权码是否有效
- 查看Django日志输出
- 测试网络连接

### 2. 验证码问题
- 检查验证码是否过期
- 确认邮箱地址匹配
- 查看数据库记录
- 检查垃圾邮件文件夹

### 3. 前端问题
- 检查API接口地址
- 确认网络请求正常
- 查看浏览器控制台
- 验证表单数据格式

## 📊 监控建议

1. **邮件发送统计**：成功率、失败原因
2. **验证码使用率**：验证成功率、过期率
3. **用户注册转化**：邮箱验证完成率
4. **安全监控**：频繁请求、恶意注册
