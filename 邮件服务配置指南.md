# 邮件服务配置指南

## 概述
本指南将帮助您为虚拟伴侣系统配置真实的邮件服务，以启用邮件验证功能。

## 支持的邮件服务提供商

### 1. QQ邮箱
```python
# 在 settings.py 中添加以下配置
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.qq.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = 'your_qq_email@qq.com'  # 你的QQ邮箱
EMAIL_HOST_PASSWORD = 'your_authorization_code'  # QQ邮箱授权码
DEFAULT_FROM_EMAIL = 'your_qq_email@qq.com'
```

**获取QQ邮箱授权码步骤：**
1. 登录QQ邮箱网页版
2. 点击"设置" → "账户"
3. 在"POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV服务"中开启SMTP服务
4. 点击"生成授权码"，按提示操作（可能需要手机验证）
5. 将生成的授权码作为 EMAIL_HOST_PASSWORD

### 2. 163邮箱
```python
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.163.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = 'your_163_email@163.com'
EMAIL_HOST_PASSWORD = 'your_authorization_code'  # 163邮箱授权码
DEFAULT_FROM_EMAIL = 'your_163_email@163.com'
```

### 3. Gmail (适用于国外用户)
```python
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = 'your_email@gmail.com'
EMAIL_HOST_PASSWORD = 'your_app_password'  # Gmail应用专用密码
DEFAULT_FROM_EMAIL = 'your_email@gmail.com'
```

## 配置步骤

### 步骤1：选择邮件服务提供商
根据您的需求选择上述任一邮件服务提供商。推荐使用QQ邮箱或163邮箱（国内用户）。

### 步骤2：获取授权码
按照对应邮件服务商的指引获取SMTP授权码。

### 步骤3：更新配置文件
编辑 `backend/Lingxi - fuben/Lingxi/settings.py` 文件：

```python
# 邮件配置 - 请替换为真实的邮件服务信息
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.qq.com'  # 替换为您的SMTP服务器
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = 'your_email@qq.com'  # 替换为您的邮箱
EMAIL_HOST_PASSWORD = 'your_authorization_code'  # 替换为您的授权码
DEFAULT_FROM_EMAIL = 'your_email@qq.com'  # 替换为您的邮箱
EMAIL_TIMEOUT = 30
```

### 步骤4：环境变量配置（推荐）
为了安全起见，建议使用环境变量存储敏感信息：

```python
import os

EMAIL_HOST_USER = os.environ.get('EMAIL_HOST_USER', 'your_email@qq.com')
EMAIL_HOST_PASSWORD = os.environ.get('EMAIL_HOST_PASSWORD', 'your_authorization_code')
DEFAULT_FROM_EMAIL = os.environ.get('DEFAULT_FROM_EMAIL', 'your_email@qq.com')
```

然后在系统环境变量中设置：
- `EMAIL_HOST_USER`: 您的邮箱地址
- `EMAIL_HOST_PASSWORD`: 您的邮箱授权码
- `DEFAULT_FROM_EMAIL`: 发送邮件的邮箱地址

### 步骤5：测试邮件发送
配置完成后，运行测试脚本验证邮件功能：

```bash
cd "e:\virtual_partner\virtual_partner\backend\Lingxi - fuben"
python test_email_verification.py
```

## 常见问题解决

### 1. 连接被拒绝 (Connection refused)
- 检查SMTP服务器地址和端口是否正确
- 确认网络连接正常
- 检查防火墙设置

### 2. 认证失败 (Authentication failed)
- 确认邮箱地址和授权码正确
- 检查是否已开启SMTP服务
- 重新生成授权码

### 3. 连接超时 (Connection timeout)
- 尝试更换SMTP端口（465或587）
- 检查网络环境是否限制SMTP连接
- 调整EMAIL_TIMEOUT设置

### 4. SSL/TLS错误
- 确认EMAIL_USE_TLS或EMAIL_USE_SSL设置正确
- 某些邮件服务商可能需要EMAIL_USE_SSL = True 和端口465

## 生产环境部署建议

### 1. 使用专业邮件服务
- 考虑使用SendGrid、AWS SES、阿里云邮件推送等专业服务
- 这些服务提供更好的送达率和监控功能

### 2. 配置域名认证
- 设置SPF记录
- 配置DKIM签名
- 添加DMARC策略

### 3. 监控邮件发送
- 监控邮件发送成功率
- 设置邮件发送失败告警
- 定期清理过期验证码

## 安全注意事项

1. **不要在代码中直接写入邮箱密码**
2. **使用授权码而不是登录密码**
3. **定期更换授权码**
4. **限制验证码发送频率**
5. **设置验证码过期时间**

## 配置验证

配置完成后，您可以：
1. 运行测试脚本检查邮件发送功能
2. 在前端页面测试注册功能
3. 尝试密码重置功能
4. 检查邮件送达情况

## 支持与帮助

如果在配置过程中遇到问题：
1. 检查Django日志文件
2. 查看邮件服务商的帮助文档
3. 确认网络和防火墙设置
4. 联系技术支持

---

配置完成后，您的虚拟伴侣系统将具备完整的邮件验证功能！
